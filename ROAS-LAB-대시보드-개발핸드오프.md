# ROAS LAB 대시보드 — 개발 핸드오프 문서

> 본 문서는 지금까지 프로토타입(`roas-lab-overview.html`)으로 확정한 대시보드 첫 화면의 구현 사양입니다.
> 프로토타입은 **프론트엔드 목업(샘플 데이터 하드코딩, SVG 직접 렌더)**이며, 아래 사양이 실제 구현의 기준입니다.
> 두 가지 **목표 사양(★)** — 기간이 모든 값에 반영 / 매체 필터 버튼 제거 — 은 프로토타입에 아직 미반영이며, 구현 시 본 문서 기준으로 처리합니다.

---

## 1. 개요

ROAS LAB은 CRM 주문 데이터 · 광고 플랫폼 지출 데이터 · GA4 행동 데이터를 통합해 **매체별 "CRM 기준 실제 ROAS"** 를 산출하는 멀티채널 광고 성과 대시보드입니다.

- **핵심 가치**: 플랫폼이 신고하는 ROAS와 CRM 실매출 기반 ROAS의 **갭**을 드러내, 예산 재배분 의사결정을 지원한다.
- **대상 사용자**: 인하우스 마케터, 광고주, 대행사(크로스채널 예산 운영자).
- **첫 화면 목적**: 로그인 직후 매체별 성과를 한눈에 비교하고, 매체 → 캠페인(utm_campaign) → 광고 분석 화면으로 드릴다운.

대상 매체: **Google · Meta · Naver · Kakao** (유료), 그 외 GA `utm_source` 기준 비유료(Organic) 소스.

---

## 2. 데이터 소스 & 파이프라인

| 소스 | 제공 데이터 | 용도 |
|---|---|---|
| **GA4** | `utm_source` / `utm_campaign`, 세션, 전환 이벤트, 노출·클릭(연동 시) | 소스/캠페인 귀속, 채널 분해 |
| **CRM (주문 데이터)** | 주문·실매출, 신규/재구매 고객, 객단가 | 실매출·ROAS·CPA·LTV 산출의 기준값 |
| **광고 플랫폼 API** | 노출·클릭·광고비 (매체·캠페인 단위) | CPM/CTR/CPC/광고비, 플랫폼 신고 ROAS |
| 커머스 플랫폼 | Cafe24 / MakeShop / 독립몰 | CRM 주문 파이프라인 연동 |

- **통합 레이어**: BigQuery 적재 → 조인 → Looker Studio/서비스 API 노출 (theCAP 엔지니어링 레이어와 공유 구조).
- **조인 키**: 주문 ↔ GA `utm_source`/`utm_campaign` 매핑, 매체별 광고비 ↔ 귀속 매출.
- **온보딩 전제**: GA 검증은 전 매체 공통 필수. Cafe24는 표준화 파이프라인으로 빠름. MakeShop/독립몰은 데이터 접근성 이슈로 추가 기간 필요. **GA 이벤트 태깅 상태와 커머스 플랫폼 유형은 독립 변수**로 취급.

---

## 3. 지표 정의 (산식)

모든 지표는 **선택된 기간(3.1)** 으로 집계한다.

### 3.1 상단 KPI 카드 (5개)

| 카드 | 정의 / 산식 | 비고 |
|---|---|---|
| **총 매출** | GA `utm_source` 전 소스의 CRM 실매출 합계 | 유료+비유료 전체 |
| **실 광고 매출** | 유료 매체(Google/Meta/Naver/Kakao) 귀속 CRM 매출 | Organic 제외 |
| **총 광고 비용** | 유료 4개 매체 광고비 합 | 플랫폼 API |
| **실 ROAS** | 실 광고 매출 ÷ 총 광고 비용 | 유료 효율 지표 |
| **예측 LTV** | 신규 고객 1인당 예측 생애가치(12개월) | 예측 모델 산출값 |

### 3.2 매체/캠페인 단위 지표 (표 · 드릴다운 · 분석화면)

| 지표 | 산식 | 좋은 방향 |
|---|---|---|
| **CRM ROAS** | 매체 CRM 매출 ÷ 매체 광고비 | 높을수록 ↑ (표 정렬 기준) |
| **광고비** | 기간 내 집행 광고비 | — |
| **CPM** | 광고비 ÷ 노출수 × 1,000 | 낮을수록 ↓ |
| **CTR** | 클릭수 ÷ 노출수 | 높을수록 ↑ |
| **CPC** | 광고비 ÷ 클릭수 | 낮을수록 ↓ |
| **CVR** | 전환수(주문) ÷ 클릭수 | 높을수록 ↑ |
| **CPA** | 광고비 ÷ 신규 획득 고객수 | 낮을수록 ↓ |
| **LTV** | 해당 채널 획득 고객 예측 생애가치 | 높을수록 ↑ |

### 3.3 파생/보조 지표

- **순규모 (콤보차트)** = 매출 − 광고비.
- **플랫폼 신고 ROAS vs CRM 실측 ROAS 갭** = (플랫폼 API 보고 ROAS) − (CRM 계산 ROAS). *제품의 핵심 내러티브. 표 상단에서는 뺐고, 드릴다운/광고 분석 화면에 배치하는 방향으로 논의됨(현재 프로토타입에서는 드릴다운의 별도 섹션은 제거된 상태 — 필요 시 분석 화면에 유지).*
- **도넛 = 매체별 매출**: GA `utm_source`별 CRM 매출. 매핑 주의: **Meta → `facebook`** (utm_source 표기).

---

## 4. 화면 사양 (컴포넌트별)

레이아웃(위→아래): 헤더 → KPI 카드 5 → 차트 2단(도넛 + 콤보) → 매체별 성과 지표 표(+인라인 드릴다운) → (드릴다운 내) 광고 분석 화면 진입.

### 4.1 헤더 & 기간 셀렉터
- 로고("ROAS LAB") + 기간 셀렉터.
- **기간 셀렉터(드롭다운)**:
  - 프리셋 4개: `최근 30일`(기본) · `최근 14일` · `최근 7일` · `어제`.
  - 커스텀: 시작·종료 날짜 직접 선택 + `적용`.
  - 기본값 **최근 30일**(어제 종료 기준). 선택된 프리셋 활성 표시.
- ★ **`매체 전체 ▾` 필터 버튼(칩)은 제거**한다.

### 4.2 KPI 카드 (5개)
- 순서: 총 매출 · 실 광고 매출 · 총 광고 비용 · 실 ROAS · 예측 LTV.
- 각 카드: 라벨 / 값(모노) / 보조 설명.
- ★ **모든 값은 선택 기간으로 재집계**된다.

### 4.3 매체별 매출 도넛
- GA `utm_source`별 매출 비중. 세그먼트를 **Paid 그룹 / Organic 그룹**으로 인접 배치.
- 색: Paid = 매체 브랜드 컬러, Organic = 뮤트 슬레이트 그레이(§7).
- 범례: `Paid · NN%` / `Organic · NN%` 두 그룹 헤더 + 소스별 행(색점·이름·%).
- 중앙 라벨: **실제 매출액**(억 단위). 기본 "전체 매출 / X.XX억", 매체 선택 시 해당 소스명 + 그 소스 매출액.
- ★ 기간에 따라 세그먼트·비중·금액 재계산.

### 4.4 일별 콤보 차트
- X축: 일자. Y: 매출/광고비(막대) + ROAS(라인).
- **막대 겹침**: 같은 x에 매출 막대(뒤, 스틸블루 #5B77B0)와 광고비 막대(앞, 페리윙클 #89B0FE)를 겹쳐, 광고비 위로 드러나는 상단 = **순규모(매출−광고비)**.
- **ROAS 라인**: 브랜드 민트 #2EE6A6 (포인트 컬러, 별도 스케일).
- **호버 툴팁**: 날짜 · 매출 · 광고비 · 순규모 · ROAS.
- 라벨 솎기: 일수가 많으면(>16일) X축 날짜 라벨 자동 간격 조정. 단일일(어제)은 라인 없이 단일 막대.
- 카드 부제에 현재 기간 범위(예: `07.06 – 08.04`) 표기.
- ★ 기간에 따라 표시 일수·값 재계산.

### 4.5 매체별 성과 지표 표
- 컬럼 순서: `# / 매체 / CRM ROAS / 광고비 / CPM / CTR / CPC / CVR / CPA / LTV`.
- **정렬**: CRM ROAS 내림차순(순위 01–04, 1위 강조).
- **CRM ROAS 셀**: 큰 수치(민트) + 미니 바(비교용).
- **컬럼별 최고 성과 셀**: 앰버(#F5A524) 텍스트. 방향 반영 → CPM·CPC·CPA는 최저, CTR·CVR·LTV는 최고.
- 표 우측 상단의 `＋ 지표 컬럼` 칩은 제거됨.
- 행 hover/선택 강조: 선택(펼침) 행은 민트 배경 틴트 + 좌측 민트 액센트 바 + 매체명/ROAS 민트.

### 4.6 인라인 드릴다운 (utm_campaign)
- 매체 행 클릭 → **해당 행 아래로 인라인 확장**(우측 드로어 아님).
- 표시: 그 매체의 `utm_campaign`들을 **동일 컬럼 구조**로 나열(들여쓰기 최소, 좌측 얇은 민트 가이드).
- 캠페인 행에도 컬럼별 최고값 앰버 하이라이트.
- 다른 매체 클릭 시 이전 확장은 닫힘(단일 오픈).
- 확장 하단 풋터: `광고 분석 화면에서 자세히 보기 →` 버튼.
- 매체 선택 시 **도넛 하이라이트**(해당 소스 강조, 나머지 딤 + 범례 딤/강조) 동반.
  - ※ 이전 버전의 "매체 선택 → 콤보차트를 해당 매체로 필터" 동작은, ★기간이 모든 값을 구동하는 구조로 전환하면서 **재검토 대상**. 구현 시: (a) 드릴다운은 표/도넛 하이라이트까지만, 콤보는 항상 전체 기준 유지 → 단순/명확. (b) 매체 선택 시 콤보도 매체 기준으로 보고 싶다면 별도 토글로 분리 권장. **기본 권장안 = (a)**.

### 4.7 광고 분석 화면 (별도 화면)
- 드릴다운의 `자세히 보기` → **새 창/새 라우트**로 이동.
- 구성: 매체 헤더 · 매체 KPI(ROAS/광고비/매출/신규 CPA/CPC/CVR) · 주간 추이(ROAS·광고비 콤보) · `utm_campaign` 성과 표(캠페인 / 광고비 / 매출 / ROAS / CPM / CTR / CPC / CVR / CPA / LTV).
- 실제 서비스 확장 여지: 소재·타겟·시간대·기기별 분해, 최적화 제안.
- 구현 노트: 프로토타입은 `window.open` + `document.write`로 자기완결 HTML을 렌더. 실제로는 라우트(`/analysis/:media?start=&end=`)로 대체 권장. 팝업 차단 케이스 처리 필요.

---

## 5. 인터랙션 사양 요약

| 트리거 | 동작 |
|---|---|
| 기간 프리셋/커스텀 선택 | ★ **KPI · 도넛 · 콤보차트 · 표 전체**를 해당 기간으로 재집계. 기간 파라미터를 모든 데이터 쿼리에 전달. |
| 매체 행 클릭 | 인라인 드릴다운(utm_campaign) 펼침/접힘 + 도넛 하이라이트 + 표 행 강조 |
| 드릴다운 `자세히 보기` | 광고 분석 화면(새 라우트/창) 진입 |
| 콤보 막대 hover | 툴팁(날짜·매출·광고비·순규모·ROAS) |
| (제거) 매체 필터 버튼 | ★ 헤더 `매체 전체 ▾` 칩 삭제 |

---

## 6. 상태 & 엣지 케이스

- **로딩**: KPI/차트/표 스켈레톤. 진행 중 표기.
- **빈 데이터(기간 내 데이터 없음)**: 각 컴포넌트 빈 상태 메시지. 콤보는 최소 1일로 폴백하거나 "데이터 없음".
- **단일일(어제)**: 콤보는 라인 없이 단일 막대, 툴팁 정상.
- **커스텀 기간**: 시작>종료 자동 정렬. 데이터 보유 범위로 min/max 제한(또는 서버측 범위 검증).
- **GA 미검증/미태깅 매체**: 해당 매체 지표 결측 처리 및 안내(온보딩 상태 노출).
- **플랫폼 API 지연/실패**: 부분 렌더 + 재시도. 플랫폼 신고 지표 결측 시 갭 계산 보류.
- **광고 분석 화면 팝업 차단**: 안내 후 재시도, 또는 동일 탭 라우팅 대안.

---

## 7. 디자인 시스템

### 컬러 토큰
| 용도 | 값 |
|---|---|
| 배경(midnight navy) | `#0A0E27` (패널 `#111634`) |
| 라인/보더 | `rgba(255,255,255,.08)` |
| 브랜드 그린(키 컬러) | `#00FF9D` |
| 화면 적용 그린(톤다운) | `#2EE6A6` (light `#7CEFC6`) — 순도 100%는 다크 UI에서 눈부심, 화면 액센트는 톤다운 값 사용 |
| 브랜드 블루(보조) | `#4F8CFF` (light `#8FB4FF`) |
| 텍스트 | `#E9ECF5` / muted `#8A92B2` / faint `#5A6288` |
| 최고값 하이라이트(앰버) | `#F5A524` |
| 콤보 — 매출 막대 | `#5B77B0` (뮤트 스틸블루) |
| 콤보 — 광고비 막대 | `#89B0FE` (라이트 페리윙클) |
| 콤보 — ROAS 라인 | `#2EE6A6` |
| 매체 브랜드 — Google | `#EA4335` |
| 매체 브랜드 — Meta(facebook) | `#0866FF` |
| 매체 브랜드 — Naver | `#03C75A` |
| 매체 브랜드 — Kakao | `#FEE500` |
| Organic 슬레이트 | `#7A82A3` / `#5C6488` / `#454B6B` |

### 타이포그래피
- UI 텍스트: **Pretendard**
- 수치/데이터: **IBM Plex Mono** (tabular-nums)

### 규칙
- "precision instrument panel" 컨셉 — 밀도 높은 계기판형 레이아웃, 모노 수치 정렬.
- ROAS = 민트(주인공/정렬 기준), 광고비/보조 = 블루 계열, 컬럼 최고값 = 앰버.
- 도넛: Paid는 매체 브랜드 컬러, Organic은 뮤트 슬레이트(시선 뺏지 않게).

---

## 8. 프로토타입 참조 & 기술 노트

- **프로토타입 파일**: `roas-lab-overview.html` (단일 파일, CSS/JS 임베드, 샘플 데이터 하드코딩).
- 차트는 외부 라이브러리 없이 **SVG 직접 렌더**로 구현됨(도넛=stroke-dasharray, 콤보=rect/polyline). 실제 구현에서는 그대로 유지하거나 차트 라이브러리(Recharts/Chart.js 등)로 대체 가능.
- 상태/데이터가 늘어나므로 **컴포넌트 프레임워크(React 등) + 기간 파라미터 상태관리** 권장.
- 브라우저 스토리지 미사용(세션 내 메모리만).

### 권장 API 계약 (예시)

```
GET /api/v1/dashboard?start=2026-07-06&end=2026-08-04
```

```json
{
  "period": { "start": "2026-07-06", "end": "2026-08-04" },
  "kpi": {
    "total_revenue": 214000000,
    "paid_ad_revenue": 152150000,
    "total_ad_spend": 32800000,
    "real_roas": 4.64,
    "predicted_ltv": 186000
  },
  "sources": [
    { "utm_source": "google",   "group": "paid",    "revenue": 57600000 },
    { "utm_source": "facebook", "group": "paid",    "revenue": 38950000 },
    { "utm_source": "(direct)", "group": "organic", "revenue": 33000000 }
  ],
  "media": [
    {
      "key": "naver", "name": "Naver",
      "crm_roas": 5.90, "spend": 7200000,
      "cpm": 4200, "ctr": 0.032, "cpc": 450, "cvr": 0.039,
      "cpa": 24000, "ltv": 228000,
      "campaigns": [
        { "utm_campaign": "브랜드 검색 캠페인", "crm_roas": 8.62,
          "spend": 1300000, "cpm": 3200, "ctr": 0.051, "cpc": 320,
          "cvr": 0.062, "cpa": 15000, "ltv": 265000 }
      ]
    }
  ],
  "timeseries": [
    { "date": "2026-07-06", "revenue": 5600000, "spend": 960000 }
  ]
}
```

> `start`/`end`(또는 프리셋 키)만 바뀌면 위 응답 전체가 해당 기간 값으로 재계산되어야 한다 (★ 5장).
> 매체 분석 화면은 `GET /api/v1/media/:key?start=&end=` 로 캠페인·추이 상세를 별도 반환.

---

## 9. 구현 체크리스트

- [ ] 기간 파라미터를 전 컴포넌트 데이터 소스에 전달 (★ KPI·도넛·콤보·표 모두 반응)
- [ ] 기간 셀렉터: 프리셋 4 + 커스텀 날짜, 기본 최근 30일, 범위 검증
- [ ] 헤더 `매체 전체 ▾` 필터 버튼 제거 (★)
- [ ] KPI 5카드 (총 매출/실 광고 매출/총 광고 비용/실 ROAS/예측 LTV)
- [ ] 도넛: utm_source 매출, Paid/Organic 그룹, 중앙 매출액 라벨, 매체 하이라이트
- [ ] 콤보: 겹친 막대(순규모) + ROAS 라인, 호버 툴팁, 라벨 솎기, 단일일 처리
- [ ] 표: 컬럼 순서/정렬/최고값 하이라이트/행 강조
- [ ] 인라인 드릴다운(utm_campaign) + 단일 오픈 + 풋터 버튼
- [ ] 광고 분석 화면 라우트 (+팝업/이동 처리)
- [ ] 지표 산식 서버측 구현 (§3) + 결측/엣지 케이스 (§6)
- [ ] 디자인 토큰 적용 (§7)

---

*문서 기준: 프로토타입 `roas-lab-overview.html`. 샘플 수치는 데모용이며 실제 값은 데이터 파이프라인 산출로 대체.*
