# ROAS LAB 대시보드 — 개발 핸드오프 문서

> 본 문서는 현재 구현된 데모 대시보드(`roas-lab-overview.html` + `analysis.html`)의 사양입니다.
> 프론트엔드는 **외부 라이브러리 없이 SVG 직접 렌더**하며, 데이터는 **외부 JSON 파일(`roas-data.json`, `roas-facts.json`)을 `fetch`** 해서 화면에서 기간·매체·피벗 기준으로 재집계합니다.
> **모든 수치는 데모용 가짜 데이터**이며, 실제 서비스에서는 데이터 파이프라인 산출값(§8의 API 계약)으로 대체합니다.
>
> *문서 기준일: 2026-08-05 (구현 반영본)*

---

## 0. 파일 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 진입점 → `roas-lab-overview.html`로 자동 이동(meta refresh) |
| `roas-lab-overview.html` | **대시보드 홈** (KPI · 도넛 · 콤보 · 매체별 성과표 · 인라인 드릴다운). `roas-data.json` 사용 |
| `analysis.html` | **광고 상세 분석** (KPI · 일/주/월 콤보 · 사용자 피벗 테이블 · CSV 다운로드). `roas-facts.json` 사용 |
| `roas-data.json` | 대시보드 홈용 **집계 데이터**(매체→캠페인→일자 중첩 구조) |
| `roas-facts.json` | 상세 분석용 **팩트 데이터**(날짜×매체×캠페인×상품 플랫 레코드, 1,020행) |
| `thecap.png`, `logo.png` | GNB 로고 이미지(thecap → logo 순) |
| `gen-data.js` | (Node) 두 JSON 재생성기 — 상단 `MEDIA`/`PRODUCTS` 수정 후 `node gen-data.js "<폴더>"` |

> **실행 전제:** `fetch`로 JSON을 읽으므로 `file://`로 직접 열면 브라우저가 로컬 fetch를 차단한다. 반드시 정적 HTTP로 서빙해야 한다(`python -m http.server 8000` → `http://localhost:8000/`). 정적 호스팅(예: 정적 페이지 서비스)에 그대로 배포 가능.

---

## 1. 개요

ROAS LAB은 CRM 주문 데이터 · 광고 플랫폼 지출 데이터 · GA4 행동 데이터를 통합해 **매체별 "CRM 기준 실제 ROAS"** 를 산출하는 멀티채널 광고 성과 대시보드입니다.

- **핵심 가치**: 플랫폼이 신고하는 ROAS와 CRM 실매출 기반 ROAS의 **갭**을 드러내, 예산 재배분 의사결정을 지원한다.
- **대상 사용자**: 인하우스 마케터, 광고주, 대행사(크로스채널 예산 운영자).
- **화면 구성(2개 + 상단 GNB)**:
  - **대시보드 홈**: 로그인 직후 매체별 성과를 한눈에 비교, 매체 → 캠페인(utm_campaign) 드릴다운.
  - **광고 상세 분석**: 날짜/매체 필터 + KPI + 일/주/월 추이 + **사용자 피벗 테이블 + 엑셀 다운로드**.
  - 두 화면은 상단 **GNB(대시보드 홈 · 광고 분석)** 로 연결.

대상 매체: **Google · Meta · Naver · Kakao** (유료), 그 외 GA `utm_source` 기준 비유료(Organic) 소스.

---

## 2. 데이터 소스 & 파이프라인

### 2.1 실제 파이프라인(목표)

| 소스 | 제공 데이터 | 용도 |
|---|---|---|
| **GA4** | `utm_source` / `utm_campaign`, 세션, 전환, 노출·클릭 | 소스/캠페인 귀속, 채널 분해 |
| **CRM (주문 데이터)** | 주문·실매출, 신규/재구매, 객단가 | 실매출·ROAS·CPA·LTV 기준값 |
| **광고 플랫폼 API** | 노출·클릭·광고비(매체·캠페인 단위) | CPM/CTR/CPC/광고비, 플랫폼 신고 ROAS |
| 커머스 플랫폼 | Cafe24 / MakeShop / 독립몰 | CRM 주문 파이프라인 연동 |

- **통합 레이어**: BigQuery 적재 → 조인 → 서비스 API 노출. **조인 키**: 주문 ↔ GA `utm_source`/`utm_campaign`, 매체별 광고비 ↔ 귀속 매출.

### 2.2 현재 데모 데이터 (구현본)

실제 API 대신 아래 두 JSON이 단일 원천이며, `gen-data.js`가 캠페인 30일 총계에서 노출/클릭/전환/신규고객/도달/세션을 파생시키고 **결정론적 해시 가중치로 일별·상품별 분해**해 생성한다(리로드해도 값 고정). 기간: **2026-07-06 ~ 2026-08-04(30일)** 고정 데모 윈도우.

**`roas-facts.json`** — 상세 분석/피벗 원천. 플랫 레코드 배열.
```jsonc
{
  "meta": { "currency": "KRW", "period_available": { "start": "2026-07-06", "end": "2026-08-04" } },
  "days": ["2026-07-06", …],
  "media": { "google": { "name":"Google","badge":"G","cls":"b-g","source":"google","color":"#EA4335" }, … },
  "rows": [
    { "date":"2026-07-06","media":"google","media_name":"Google","source":"google",
      "utm_campaign":"검색_브랜드","product_id":"P0005","product_name":"프리미엄 러닝화",
      "spend":…, "custom_spend":…, "revenue":…, "impressions":…, "reach":…,
      "clicks":…, "ga_sessions":…, "orders":…, "new_customers":… }
  ]
}
```

**`roas-data.json`** — 대시보드 홈용 집계본(위 팩트를 캠페인 단위로 합산). 매체→캠페인→`daily[{date,spend,revenue,impressions,clicks,conversions,new_customers}]` 중첩 + `organic[{utm_source,color,daily[{date,revenue}]}]`.

> 두 파일은 같은 생성기에서 나와 **총계가 정합**한다(총매출 ≈ ₩214,000,000, 실 ROAS 464%). 집계 규칙: **매체 = 캠페인 합, 전체 = 매체 합**, ROAS = 매출 ÷ 광고비, 각 비율지표 = 집계 카운트의 비율.

---

## 3. 지표 정의 (산식)

모든 지표는 **선택 기간**(홈: 기간 셀렉터 / 상세: 날짜 필터)으로 집계한다.

### 3.1 대시보드 홈 상단 KPI 카드 (5개, 순서 = 실 ROAS 우선)

| 카드 | 정의 / 산식 | 비고 |
|---|---|---|
| **실 ROAS** | 실 광고 매출 ÷ 총 광고 비용 | 첫 번째 카드(키 컬러 강조) |
| **총 매출** | GA 전 소스 CRM 실매출 합 | 유료+비유료 |
| **실 광고 매출** | 유료 4개 매체 귀속 CRM 매출 | Organic 제외 |
| **총 광고 비용** | 유료 4개 매체 광고비 합 | 플랫폼 API |
| **예측 LTV** | 신규고객 가중평균 예측 생애가치 | 매체별 신규고객수로 가중평균(≈₩200,000) |

### 3.2 매체/캠페인 단위 지표

| 지표 | 산식 | 좋은 방향 |
|---|---|---|
| **CRM ROAS** | 매출 ÷ 광고비 | 높을수록 ↑ (정렬 기준) |
| **CPM** | 광고비 ÷ 노출수 × 1,000 | 낮을수록 ↓ |
| **CTR** | 클릭수 ÷ 노출수 | 높을수록 ↑ |
| **CPC** | 광고비 ÷ 클릭수 | 낮을수록 ↓ |
| **CVR** | 주문수 ÷ 클릭수 | 높을수록 ↑ |
| **CPA** | 광고비 ÷ 신규고객수 | 낮을수록 ↓ |
| **LTV** | 채널 획득 고객 예측 생애가치 | 높을수록 ↑ |

### 3.3 상세 분석 전용 지표/차원

- **광고비(Custom)**: 수기 조정 광고비(파생값). **도달수(reach)**: 순 도달(노출 ≥ 도달). **GA세션수**: GA 유입 세션. **주문수 / 신규고객수**: 카운트.
- **차원**: 날짜(월/일), 매체, 소스, 캠페인, 상품명, 상품ID.

### 3.4 파생/내러티브

- **도넛 = 매체별 매출**: GA `utm_source`별 CRM 매출. 매핑 주의: **Meta → `facebook`**.
- 플랫폼 신고 ROAS vs CRM 실측 ROAS 갭 = 제품의 핵심 내러티브(상세 분석 확장 여지).

---

## 4. 화면 사양 (컴포넌트별)

### 4.0 GNB (상단 글로벌 내비 · 양 화면 공통)
- 좌측 **로고 이미지**(`thecap.png` → `logo.png` 순) + 메뉴 **`대시보드 홈` · `광고 분석`**.
- 현재 화면 메뉴 활성 표시(민트). `광고 분석` 클릭 시 **매체 필터 없이 전체**로 상세 화면 진입.

### 4.1 대시보드 홈 — 헤더 & 기간 셀렉터
- 기간 셀렉터(드롭다운): 프리셋 `최근 30일`(기본) · `14일` · `7일` · `어제` + 커스텀(시작·종료 + 적용). 팝업은 우측 정렬(박스 밖으로 잘리지 않게).

### 4.2 KPI 카드 (5개)
- 순서: **실 ROAS**(민트) · 총 매출 · 실 광고 매출 · 총 광고 비용 · 예측 LTV.
- ★ 모든 값은 선택 기간으로 재집계. 카드 좌우 패딩(24px)을 본문과 정렬해 아래 콘텐츠와 같은 라인에서 시작.

### 4.3 매체별 매출 도넛
- **도넛(좌) + 범례(우)** 배치. 범례는 **Paid / Organic 두 그룹을 세로로 스택**(그룹 헤더 `Paid · NN%` / `Organic · NN%` + 소스별 색점·이름·%).
- 색: Paid = 매체 브랜드 컬러, Organic = 뮤트 슬레이트. 중앙 라벨: 전체 매출(억), 매체 선택 시 해당 소스명 + 금액.
- ★ 기간에 따라 세그먼트·비중·금액 재계산. 매체 드릴다운 시 해당 소스 강조(나머지 딤).

### 4.4 일별 콤보 차트
- X: 일자. Y: 매출 막대(스틸블루 #5B77B0, 뒤) + 광고비 막대(페리윙클 #89B0FE, 앞) 겹침 + **ROAS 라인**(민트 #2EE6A6, 별도 스케일).
- 매출 = **전 소스(총매출) 기준**. 호버 툴팁: 날짜·매출·광고비·순규모·ROAS. 라벨 솎기(>16일). 단일일은 라인 없이 단일 막대.
- **범례(매출/광고비/ROAS)를 카드 제목과 같은 라인**에 배치(높이 절약). 카드 부제에 기간 범위 표기.
- ★ 기간에 따라 재계산. **매체 드릴다운 시 콤보를 해당 매체 기준으로 필터**(§4.6, 옵션 b 채택) — 제목 옆 `○○ 필터 ✕` 칩으로 해제.

### 4.5 매체별 성과 지표 표
- 컬럼: `# / 매체 / CRM ROAS / 광고비 / CPM / CTR / CPC / CVR / CPA / LTV`. **CRM ROAS 내림차순**(1위 강조).
- CRM ROAS 셀: 큰 수치 + 미니 바. **컬럼별 최고 성과 = 앰버**(방향 반영: CPM·CPC·CPA 최저 / CTR·CVR·LTV 최고).
- **키 컬러(민트) 규칙**: 화면에 항상 하나의 "최고 ROAS"만 강조.
  - 드릴다운 닫힘 → **최고 ROAS 매체** 행에 민트.
  - 드릴다운 열림 → 열린 매체의 캠페인 중 **최고 ROAS 캠페인에만** 민트, **매체 행의 민트는 모두 제거**(인디고/중립 처리).

### 4.6 인라인 드릴다운 (utm_campaign)
- 매체 행 클릭 → **해당 행 아래 인라인 확장**(동일 컬럼 구조, 좌측 얇은 민트 가이드). 단일 오픈.
- 동반 동작: **콤보 매체 필터(옵션 b)** + 도넛 하이라이트 + 필터 칩.
- 확장 풋터 `광고 분석 화면에서 자세히 보기 →` → `analysis.html?media=<매체>&start=&end=` **새 창**(팝업 차단 시 같은 탭 이동). 해당 매체·기간이 선택된 상태로 진입.

### 4.7 광고 상세 분석 화면 (`analysis.html`)
독립 페이지. 상단 GNB + 헤더(제목 + **날짜·매체 필터**) → KPI → 콤보 → 피벗 테이블.

- **필터**
  - 날짜: 프리셋(30/14/7/어제) + 커스텀.
  - 매체: **다중 선택 체크박스**(전체/개별). 버튼 라벨은 선택 매체를 **뱃지+이름으로 전부** 표시, 전체 선택 시 텍스트 `전체`. 드릴다운으로 진입 시 해당 매체 프리셀렉트, GNB `광고 분석` 진입 시 전체.
- **KPI (7개)**: 큰 카드 **실 ROAS · 매출액 · 광고비 · 주문수** + 작은 카드 **CPM · CPC · CPA**.
- **콤보 차트**: 홈과 동일 형식 + **일 / 주 / 월 집계 토글**(세그먼트). 주=주 시작일 버킷, 월=월 버킷.
- **사용자 피벗 테이블**
  - 좌측 **피벗 기준(체크박스, 다중)**: `날짜(월)`=`Y년 M월`, `날짜(일)`=`Y년 M월 D일`, `매체`, `소스`, `캠페인`, `상품명`, `상품ID`. 선택 차원으로 그룹핑.
  - 상단 **표시 컬럼(토글 칩)**: 매출액 · 광고비 · 광고비(Custom) · ROAS · CPM · CTR · CPC · CVR · CPA · 노출수 · 도달수 · 클릭수 · GA세션수 · 주문수 · 신규고객수. **칩 드래그로 컬럼 순서 변경**(표·CSV에 반영), 클릭으로 표시/숨김.
  - 헤더 클릭 **정렬**(기본 ROAS ▼), 행 수 배지(`N건`).
  - 표는 **가로·세로 스크롤**(헤더 sticky, `max-height` 세로 스크롤).
  - **엑셀 다운로드**: 현재 보이는 표(활성 차원·컬럼·정렬 반영)를 **UTF-8 BOM CSV**로 저장(숫자는 raw, 한글 정상). 파일명 `ROAS_상세데이터_<start>_<end>.csv`.

---

## 5. 인터랙션 사양 요약

| 트리거 | 동작 |
|---|---|
| GNB `대시보드 홈` / `광고 분석` | 화면 이동. `광고 분석`은 매체 전체. |
| 기간/날짜 프리셋·커스텀 | ★ KPI·도넛·콤보·표(홈) / KPI·콤보·피벗(상세) 전체 재집계 |
| (홈) 매체 행 클릭 | 인라인 드릴다운 펼침/접힘 + 콤보 매체 필터 + 도넛 하이라이트 + 표 행/키컬러 갱신 |
| (홈) 드릴다운 `자세히 보기` | 광고 상세 분석(새 창, 매체·기간 프리셀렉트) |
| (상세) 피벗 기준 체크 | 그룹핑 차원 추가/제거 → 표 재계산 |
| (상세) 표시 컬럼 칩 클릭 / 드래그 | 컬럼 표시·숨김 / 순서 변경 |
| (상세) 헤더 클릭 | 해당 컬럼 정렬(민트 방향 표시) |
| (상세) 엑셀 다운로드 | 현재 표를 CSV로 저장 |
| 콤보 막대 hover | 툴팁(날짜·매출·광고비·순규모·ROAS) |

---

## 6. 상태 & 엣지 케이스

- **로딩/실패**: `fetch` 실패(예: `file://`) 시 안내 배너 노출(“HTTP로 서빙 필요”).
- **빈 데이터**: 콤보/표 빈 상태 메시지. 콤보는 최소 1일 폴백.
- **단일일(어제)**: 콤보 라인 없이 단일 막대.
- **커스텀 기간**: 시작>종료 자동 정렬, 데이터 보유 범위(min/max)로 제한.
- **상세 매체 필터**: 최소 1개 유지(0개 방지).
- **팝업 차단**: 상세 화면 `window.open` 실패 시 같은 탭 이동으로 폴백.

---

## 7. 디자인 시스템

### 컬러 토큰
| 용도 | 값 |
|---|---|
| 배경(midnight navy) | `#0A0E27` (패널 `#111634`, GNB `#0b0f28`) |
| 라인/보더 | `rgba(255,255,255,.08)` |
| 브랜드 그린(키 컬러, 화면 적용) | `#2EE6A6` (light `#7CEFC6`) |
| 브랜드 블루(보조) | `#4F8CFF` (light `#8FB4FF`) |
| 텍스트 | `#E9ECF5` / muted `#8A92B2` / faint `#5A6288` |
| 최고값 하이라이트(앰버) | `#F5A524` |
| 콤보 — 매출/광고비/ROAS | `#5B77B0` / `#89B0FE` / `#2EE6A6` |
| 매체 브랜드 — Google/Meta(facebook)/Naver/Kakao | `#EA4335` / `#0866FF` / `#03C75A` / `#FEE500` |
| Organic 슬레이트 | `#7A82A3` / `#5C6488` / `#454B6B` |

### 타이포그래피
- UI 텍스트: **Pretendard**
- 수치/데이터: **Reddit Mono** (tabular, 가독성 개선을 위해 IBM Plex Mono에서 교체). 한글 단위가 섞이는 배지 등은 `Pretendard` fallback 지정. 숫자 카드에는 한글 단위('건' 등)를 붙이지 않는다.

### 규칙
- "precision instrument panel" — 밀도 높은 계기판형, 모노 수치 정렬.
- 키 컬러(민트)는 **항상 단 하나의 최고 ROAS**만 강조(§4.5). 보조/딤 = 인디고, 컬럼 최고값 = 앰버.
- 도넛: Paid는 매체 브랜드 컬러, Organic은 뮤트 슬레이트.
- 컨테이너 최대폭 1320px(두 화면 공통).

---

## 8. 기술 노트 & API 계약

- 두 화면 모두 **외부 차트 라이브러리 없이 SVG 직접 렌더**(도넛=stroke-dasharray, 콤보=rect/polyline). 확장 시 프레임워크(React 등) + 상태관리 권장.
- 데이터는 `fetch`로 로드하므로 **HTTP 서빙 필수**. 값 변경은 `gen-data.js`의 `MEDIA`/`PRODUCTS` 수정 후 재생성.
- 브라우저 스토리지 미사용(세션 내 메모리만).

### 권장 API 계약 (실서비스 전환 시)
```
GET /api/v1/dashboard?start=2026-07-06&end=2026-08-04        → 홈(roas-data.json 대체)
GET /api/v1/facts?start=&end=&media=                         → 상세 팩트(roas-facts.json 대체)
```
> `start`/`end`(및 `media`)만 바뀌면 응답 전체가 해당 기간·매체로 재계산되어야 한다(★ 5장). 상세 화면 피벗은 팩트 응답을 클라이언트에서 그룹핑/집계한다.

---

## 9. 구현 체크리스트 (현재 상태)

- [x] 기간이 전 컴포넌트 재집계 (★ 홈·상세 공통)
- [x] 기간/날짜 셀렉터: 프리셋 4 + 커스텀, 범위 검증
- [x] 헤더 `매체 전체 ▾` 필터 칩 제거 (★)
- [x] 홈 KPI 5카드(실 ROAS 우선) + 본문 정렬
- [x] 도넛(범례 우측 · Paid/Organic 세로) + 매체 하이라이트
- [x] 콤보(겹친 막대 + ROAS 라인, 범례 제목 라인, 매체 필터 옵션 b, 툴팁)
- [x] 표: 정렬/최고값 앰버/단일 키컬러 규칙/행 강조
- [x] 인라인 드릴다운(utm_campaign) + 단일 오픈 + 풋터 진입
- [x] GNB(대시보드 홈 · 광고 분석) + 로고 이미지
- [x] 광고 상세 분석 화면(`analysis.html`): 날짜·매체 필터, KPI 7, 일/주/월 콤보
- [x] 사용자 피벗 테이블(차원 다중 선택 · 컬럼 토글/드래그 · 정렬 · 가로·세로 스크롤)
- [x] 엑셀(CSV) 다운로드
- [x] 데모 데이터 JSON 2종 + 생성기(`gen-data.js`)
- [x] 수치 폰트 Reddit Mono 교체
- [ ] (실서비스) §8 API 연동 + 결측/온보딩 상태 처리
- [ ] (확장) 상세 화면: 소재·타겟·시간대·기기 분해, 컬럼 헤더 직접 드래그 재정렬

---

*문서 기준: 구현본 `roas-lab-overview.html` + `analysis.html`. 샘플 수치는 데모용이며 실제 값은 데이터 파이프라인 산출로 대체.*
